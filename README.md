# Nginx-webpage

This sets up Nginx and a HTML page via a virtual machine.

## Steps

-Install Vagrant and VirtualBox.

-Initialize and set up the Vagrantfile

-Create the Nginx bash script.

-Link the bash script to the Vagrantfile.

-Run vagrant up or vagrant provision(when rerunning).

-Run http://localhost:8080/ in your browser.
